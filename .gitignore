# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.so
*.egg-info/
dist/
build/
investigation/
.investigations/moralstack.db
moralstack.db-*
benchmark_outputs/

# Runtime artifacts (DB, probe outputs, root logs) — never commit these
/moralstack.db
/moralstackbkp.db
/logs/
risk_probe_out.*

# Virtual environments
.venv/
venv/
env/

# Pytest
.pytest_cache/
.coverage
coverage.xml
htmlcov/

# mypy
.mypy_cache/

# Logs & reports
.investigations/logs/
# Only ignore a top-level `reports/` output dir. A bare `reports/` pattern matches
# any directory named `reports` anywhere (including `moralstack/reports/`).
/reports/
.debug/
*.pyc

# IDE
.vscode/
.idea/

# Claude Code — condividi gli artefatti team, tieni locale il personale
.claude/*
!.claude/settings.json
!.claude/rules/
!.claude/agents/
!.claude/hooks/
!.claude/skills/
.claude/settings.local.json
.claude/.instructions-loaded.log
.claude/.session-edits.json
.claude/.last-verified.json
.claude/.nudge-count.json
.claude/.context-snapshot.md
.claude/.docs-stub.md
.claude/session-diary.md

# AI harness — generated, reproducible artifacts (keep plans/handoffs/review summaries)
ai/reviews/diff-*.md
ai/prompts/generated-*.md

# OS
.DS_Store
Thumbs.db

# Environment (secrets)
.env
